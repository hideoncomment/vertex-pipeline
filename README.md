# Introduction 
This project aims to automatize the process of creating and scheduling pipelines on Google Cloud Vertex AI Pipelines.

# Build and Test
After creating the virtual environment, first you need to install all the dependencies by running the following commando `pip install -r requirements`. After that you can simply run `make run_train` or `make run_predict` to launch a pipeline on Vertex, or if you want to 